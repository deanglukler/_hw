### remove a file from staging

git reset -- fileName

--

!! do not fuck with this on a repo that doesn't have any commits yet

git reset --hard head

### --soft keeps all changes staged

git reset --soft head~

### --mixed keeps all changes but removes them from staging

git reset --mixed head~

// reset git to the original head commit

git reset --hard ORIG_HEAD



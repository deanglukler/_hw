// if you want to remove the file only from the Git repository and not remove it from the filesystem

git rm --cached file1.txt

// or for a directory

git rm -r --cached .vscode

// remove everything from being tracked

git rm -r --cached .

---

// this will not only remove the actual files from disk, but will also stage the removal of the files

git rm


// remove untracked files from repo

git clean -n




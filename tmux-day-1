cheatsheet: https://gist.github.com/MohamedAlaa/2961058

# session
# new session with name
tmux new -s [name of session]

# attach to name
tmux a -t [name of session or number]

# swith
tmux switch -t session_name

# kill session
tmux kill-session -t targetSession


# If you are inside a tmux session you would like to keep, use 
tmux kill-session -a 
# to close all other sessions







# Panes

# To split a pane horizontally:
ctrl+b "

# To split pane vertically:
ctrl+b %

# switch panes
ctrl+b [arrow key]

# expand the pane down a few lines
ctrl+b :

# from cheatsheet : 
Panes (splits)
%  vertical split
"  horizontal split

o  swap panes
q  show pane numbers
x  kill pane
+  break pane into window (e.g. to select text by mouse to copy)
-  restore pane from window
⍽  space - toggle between layouts
<prefix> q (Show pane numbers, when the numbers show up type the key to goto that pane)
<prefix> { (Move the current pane left)
<prefix> } (Move the current pane right)
<prefix> z toggle pane zoom







